---
description: Data model conventions for each tracked domain — apply when working on schema, migrations, or Supabase queries
globs: ["supabase/**", "lib/supabase/**"]
alwaysApply: false
---

# Data Model

Every table includes: `id uuid pk default gen_random_uuid()`, `created_at timestamptz default now()`, `updated_at timestamptz default now()`, `updated_by` (references auth.users, nullable while single-user).

## 1. Kitchen inventory (`kitchen_inventory`)
- item_name text
- category text
- unit text (kg, litre, pack, etc.)
- current_qty numeric
- reorder_level numeric
- reorder_qty numeric
- supplier text
- cost_per_unit numeric
- last_restocked_at date
- notes text

Derived state: `status = current_qty <= reorder_level ? 'low' : 'ok'` — computed, not stored.

## 2. IT equipment register (`it_equipment`)
- asset_tag text unique
- item_name text
- category text (laptop, monitor, router, printer, etc.)
- assigned_to text
- serial_number text
- purchase_date date
- warranty_expiry date
- status text (active, in_repair, retired)
- location text
- notes text
- warranty_card_url text (Supabase Storage path — warranty card photo/PDF)

## 2b. Appliances register (`appliances`)
Same shape as IT equipment (asset tag, name, category, assignment, serial, purchase/warranty dates, status, location, notes) plus:
- site text (`clifton_office` | `gondpass_mill`) — two registers: Clifton Office and GondPass Mill. Unique on (site, asset_tag).
- warranty_card_url text (Supabase Storage path — warranty card photo/PDF)

Categories are site appliances (AC, fridge, microwave, etc.). One warranty card file per item. Uploads go to the `warranty-cards` bucket (dashboard only; the chat agent cannot attach files). Chat can create/update/delete appliances via `appliances_*` tools and must set `site`.

## 3. Generator (`generator_maintenance`, `generator_fuel_log`, `generator_run_log`, `generator_expenses`, `generator_vendors`)
Dashboard subsections: Maintenance, Outage/Generator Runs, Expenses, Fuel, Vendor.

`generator_maintenance`:
- service_date date
- next_service_due date (default: service_date + 1 month — monthly checkup cadence)
- service_type text
- vendor text (display name; not a FK — pick from `generator_vendors` or type freely)
- cost numeric
- notes text
- checkup_status text (`done` | `not_done`) — whether this checkup was completed

`generator_run_log` (manual outage runs — not live telemetry):
- run_date date
- hours_run numeric
- started_at timestamptz
- ended_at timestamptz
- notes text

`generator_fuel_log`:
- log_date date
- liters_added numeric
- running_hours numeric
- fuel_level_pct numeric
- cost numeric
- notes text

`generator_expenses` (ledger / fuel expense sheets):
- expense_date date
- account text
- description text
- debit numeric (summed for total expense)
- credit numeric (optional)
- notes text

`generator_vendors` (people responsible for generator maintenance):
- name text (unique, case-insensitive)
- phone text
- notes text

Site date display/parse convention: DD/MM/YYYY (stored as ISO dates).

## 4. Solar system (`solar_specs`, `solar_monitoring_log`, `solar_maintenance`)
Dashboard plants (display names): Good We Office, Sungrow Office, KMP Home Solar.

`solar_specs` (effectively one row, but keep as a table for history):
- panel_capacity_kw numeric
- inverter_model text
- battery_capacity_kwh numeric
- install_date date
- vendor text
- warranty_expiry date
- spec_file_url text (Supabase Storage path — original Word/PDF)

`solar_monitoring_log`:
- log_date date
- generation_kwh numeric
- consumption_kwh numeric
- battery_soc_pct numeric
- alert_flag boolean default false
- notes text

`solar_maintenance` (service / maintenance per plant — dashboard Solar → Service):
- site_id text (plant slug: `kafi-commodities` | `sungrow-office` | `nizam-energy`)
- service_date date
- next_service_due date (default: service_date + 1 month)
- service_type text
- vendor text
- cost numeric
- notes text
- checkup_status text (`done` | `not_done`)

`solar_net_metering_logs` (KE credit ledger — dashboard Solar → Net Metering; one ledger per plant):
- solar_site_id text (same plant slugs as maintenance)
- ke_account_number, consumer_name, bill_period_label, bill_month
- previous_balance_rs, net_metering_rs, consumed_rs
- gross_balance_rs, refund_rs, net_balance_rs, estimated_refund_rs
- units_import_kwh, units_export_kwh, payable_rs
- bill_file_path, ai_extraction, ai_narrative, sems_export_kwh, notes

## 5. Internet & utility accounts (`utility_accounts`)
- utility_type text (internet, electricity, gas, water, mobile)
- provider text — dashboard sections: K-Electric (4 sites), SSGC (2), KWSB Clifton, Water tanker (Home, Office, 239G Mill, 234G Mill), Drinking water (Clifton Office), PTCL (2), Jazz (2)
- account_number text
- billing_cycle text
- monthly_avg_cost numeric
- due_date_day int (day of month bill is due)
- contact_person text
- notes text
- **never store passwords/credentials in this table or anywhere in the app.** Reference a password manager entry name only.

`utility_payment_logs` (bills / payments per account):
- utility_account_id uuid → utility_accounts
- paid_on date
- amount numeric
- units_kwh numeric
- bill_period text
- invoice_number text
- bill_file_url text
- notes text
- ai_summary text (AI report: why this bill, vs previous, difference)
- ai_summary_model text
- ai_summary_at timestamptz

## 6. Tenants (`tenants`, `tenant_rent_line_items`, `tenant_rent_schedule`, `tenant_rent_payments`, `tenant_electric_bills`)
`tenants` (account + contract terms):
- tenant_name text (unique, case-insensitive)
- survey_no text
- whatsapp_number text (international digits, e.g. 923001234567; click-to-chat rent reminders)
- contract_start_date / contract_end_date date (lease period; monthly ledger is generated from overlapping calendar months)
- security_deposit_amount numeric
- security_deposit_bank_account / security_deposit_bank_name / security_deposit_cheque_no text
- sqft / rate numeric
- rate_type text (`per_sqft` | `lum_sum`)
- gross_rent numeric (`sqft * rate` when per_sqft; flat amount when lum_sum)
- contract_detail text
- agreement_file_url text (agreement PDF/photo in `tenant-documents` bucket)
- notes text

Deprecated snapshot columns may still exist (`rent_amount`, `rent_due_date`, `payment_status`, `payment_date`, `outstanding_amount`, `agreement_expiry`, `payment_file_url`) — do not write them from the UI; outstanding and due state are computed from the ledger.

`tenant_rent_line_items` (extra monthly columns such as Office Rent):
- tenant_id uuid → tenants
- label text
- amount numeric
- sort_order int

`tenant_rent_schedule` (one row per calendar month in the contract):
- tenant_id uuid → tenants
- serial_no, period_year, period_month
- period_start / period_end date
- snapshot of survey_no, sqft, rate, rate_type, gross_rent
- line_items jsonb (`[{ label, amount }]`)
- withholding_tax / withholding_tax_received numeric (official tenants)
- total_due numeric (gross_rent + line items — not net of WHT)
- balance = total_due − rent received + (withholding_tax − withholding_tax_received)

`tenant_rent_payments` (receipts against a schedule month):
- schedule_id uuid → tenant_rent_schedule
- amount_received numeric
- payer_bank_account / payer_bank_name (from)
- payee_bank_account / payee_bank_name (to)
- cheque_no / payment_reference text
- payment_file_url text

`tenant_brokers` (commission slips):
- broker_name text
- tenant_id uuid → tenants
- sqft / rate numeric (copied from the tenant, editable)
- monthly_rent numeric (sqft × rate, or tenant gross rent)
- stay_months / stay_days / stay_factor (from contract From/To; leftover days / 30)
- commission_amount numeric — (monthly_rent / 12) × stay_factor
- notes text

`tenant_electric_bills` (K-Electric charges billed to the tenant — not site utility meters):
- tenant_id uuid → tenants
- period_from / period_to date (billing From/To; months derived as calendar-month delta)
- months numeric (derived)
- last_reading / current_reading numeric
- consumed_units numeric (derived: current − last)
- rate_inclusive_govt numeric
- ke_charges_amount numeric (Amount — derived: consumed_units × rate)
- amount_received numeric
- payment_date date (date received)
- outstanding_amount numeric (derived: max(0, amount − amount_received))
- payment_status text (`paid` | `unpaid` | `partial` | `overdue` — derived from amount vs received; overdue when not paid and period_to past)
- due_date date (synced to period_to for back-compat / alerts)
- bill_file_url text (bill PDF/photo in `tenant-documents` bucket, kind `electric-bills`)
- notes text

## 7. Chart of Accounts (`chart_of_accounts_entries`)
Subsidiary ledgers (one table, `ledger` discriminator):
- ledger text — `solar_panel_clifton` | `eobi` | `k_electric_gondpass` | `kwsb_clifton`
- entry_date date
- ref_no text (e.g. CPV-74, J-511)
- account_description text (Accounts / Description column)
- document_no text (Document # / narration)
- debit numeric default 0
- credit numeric default 0
- notes text

Derived: running balance = prior balance + debit − credit (computed in UI / agent, not stored).

## Adding a new domain later
Follow the same shape: base audit columns + domain fields + one `..._log` table if the domain has recurring events (like fuel or monitoring). Don't invent a different pattern.
